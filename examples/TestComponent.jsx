import React, {PropTypes} from 'react';

const TestComponent = ({actions, mitigatedCiders, cloudSignalingSettings, alertStatus}) => (

);

TestComponent.propTypes = {
  actions: PropTypes.,
  mitigatedCiders: PropTypes.,
  cloudSignalingSettings: PropTypes.,
  alertStatus: PropTypes.,
};

export default TestComponent;
