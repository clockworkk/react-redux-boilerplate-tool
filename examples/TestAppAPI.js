import xr from 'xr';

export default class TestAppAPI {
  constructor() {
    this.baseUri = 'replace_with_uri';
    this.acceptJsonHeader = {headers: {Accept: 'application/json'}};
  }
}

export default TestAppAPI;